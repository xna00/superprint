{
  "name": "server",
  "version": "1.0.0",
  "type": "module",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc --emitDeclarationOnly",
    "buildtype": "npx tsc --emitDeclarationOnly",
    "bundle": "npx tsc && npx esbuild main.ts --bundle --sourcemap --platform=node --format=esm --outfile=bundle.js",
    "start": "node --experimental-strip-types main.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^22",
    "@types/pdfkit": "^0.17.6",
    "@types/ws": "^8.18.1",
    "@wecom/crypto": "^1.0.1",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@types/uzip": "^0.20201231.2",
    "jsonrepair": "^3.14.0",
    "pdfkit": "^0.18.0",
    "uzip": "^0.20201231.0",
    "ws": "^8.20.0"
  }
}
